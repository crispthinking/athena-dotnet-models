mode: ContinuousDelivery

branches:
  master:
    regex: ^main$
    label: ''
  hotfix:
    regex: ^hotfix(es)?[/-]
    mode: ContinuousDelivery
    increment: None
    label: beta
  feature:
    regex: ^(feature|feat)[/-]
    label: beta
    mode: ContinuousDelivery
